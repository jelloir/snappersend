[Unit]
Description=di-snapsend — Snapper-native Btrfs snapshot replication to the destination host
Documentation=file:/usr/local/share/doc/di-snapsend/README.md
# Need the network up before we can ssh the stream across.
Wants=network-online.target
After=network-online.target

[Service]
Type=oneshot
# Runs as root: btrfs send needs privilege, and the transport ssh key + log live
# under root-owned paths. The destination side runs as the least-privileged 'snapsend' user.
User=root
ExecStart=/usr/local/bin/di-snapsend
# Be a good neighbour on a workstation: snapshots are a background chore.
Nice=10
IOSchedulingClass=idle
CPUSchedulingPolicy=batch
# A full seed can take a long time; never time it out mid-stream.
TimeoutStartSec=0
# di-snapsend already takes a flock; this is belt-and-braces.
# A transient failure (destination asleep, network down) should not spam-restart.
Restart=no
